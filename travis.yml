language: node_js
node_js:
  - "7"
dist: trusty
branches:
  only:
    - vue

before_script:
  - npm install

script:
  - npm run build

after_success:
    - firebase deploy --token=${FIREBASE_API_TOKEN} -P staging
